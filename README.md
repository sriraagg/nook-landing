# Nook landing page

Static landing page for Nook, deployed through Cloudflare Pages from the public `nook-landing` repository.

The waitlist form posts to Nook’s public API. Keep the API origin and allowed CORS origin aligned with the production domain before launch.
